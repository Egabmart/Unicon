<?xml version = "1.0" encoding = "ISO-8859-1"?>

<!DOCTYPE controllers SYSTEM "dtds/controllers.dtd">

<controllers>

    <controller name="SignIn">
        <action name="signInAlumno"
                class="org.example.unicon.accions.ConexionAction"/>
        <action name="signInProfesor"
                class="org.example.unicon.accions.ConexionActionProf"/>
    </controller>

    
    <!-- Environment variable:
    <env-var name="unicon_DEFAULT_YEAR" value="2015"/> 
    -->
    
    <!-- Session object:
    <object name="unicon_activeYear" class="java.lang.Integer" value="2015" 
    	    scope="global"/>    
    -->
    
    <!-- Controller:
    <controller name="Teacher">
    	<extends controller="Typical"/>
    	<action name="new" icon="library-plus" keystroke="Control N"
        	class="actions.CreateTeacher">
    	</action>
	</controller>    
    -->
    
</controllers>